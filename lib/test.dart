import 'package:flutter/material.dart';

class justTesting extends StatelessWidget {
  const justTesting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
